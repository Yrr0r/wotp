{
  "name": "wotp",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "2.6.2"
  },
  "private": true,
  "scripts": {
    "test": "echo 'noscript, exit.'",
    "build": "babel src -d lib"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "totp-generator": "^0.0.14"
  }
}
